package com.mentormateacademy.flashcardmobileclient;

import android.os.Bundle;
import android.support.annotation.Nullable;
import android.support.v4.app.Fragment;
import android.view.LayoutInflater;
import android.view.View;
import android.view.ViewGroup;

public class ExampleDeckFragment extends Fragment {

    private static final int DECK_LOADER_INDEX = 0;

    @Override
    public View onCreateView(LayoutInflater inflater, @Nullable ViewGroup container, @Nullable Bundle savedInstanceState) {
//        View fragmentView = inflater.inflate(R.layout.deck_layout, container, false);
//
//        // get list component
//        ListView decksListView = (ListView) fragmentView.findViewById(R.id.decks_list_view);
//
//        // set adapter
//        DeckAdapter deckAdapter = new DeckAdapter(getActivity(), null);
//        decksListView.setAdapter(deckAdapter);
//
//        // start loader
//        DeckCursorLoader cursorLoader = new DeckCursorLoader(getActivity(), deckAdapter);
//        getLoaderManager().initLoader(DECK_LOADER_INDEX, null, cursorLoader);

        //return fragmentView;

        return null;
    }
}
